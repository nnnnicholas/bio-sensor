var bufferStart = new Date();

console.log(bufferStart);

setTimeout(readSensor, 3000); // read sensor every 3 seconds

function readSensor(){
	// read the sensor
	// record to buffer
	// if buffer full, save to file
	// if out of range 
}